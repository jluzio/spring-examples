spring:
  application:
    name: config-server

logging:
  level:
    root: info
    com.example: debug
    org.springframework: debug

server.port: 8888
#spring.cloud.config.server.git.uri: file://${user.home}/config-repo
spring.cloud.config.server.git.uri: file:/e:/home/config-repo/config-examples

#spring.security.user:
#  name: root
#  password: s3cr3t
