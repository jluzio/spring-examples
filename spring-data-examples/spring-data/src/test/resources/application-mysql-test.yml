spring:
  docker.compose:
    enabled: true
    skip.in-tests: false
    file: docker/mysql.docker-compose.yml
  jpa.hibernate.ddl-auto: update
  datasource:
    url: jdbc:mysql://localhost:3306/test
    username: test
    password: test
