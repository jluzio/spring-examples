example-service:
  settings:
    enabled: true
    name: example-service
    endpoint: http://example-service:8080/v1
    features:
      text:
        name: text
        values:
          from: "+351000000000"
      email:
        name: email
        values:
          from: "john.doe@server.com"
